import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: false, // Disable strict mode to avoid double API calls in dev
  /* other config options */
};

export default nextConfig;
